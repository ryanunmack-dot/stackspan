{
  "name": "stackspan",
  "version": "1.0.0",
  "private": true,
  "description": "StackSpan foot health and minimalist footwear guides",
  "scripts": {
    "dev": "wrangler pages dev public",
    "deploy": "wrangler pages deploy public",
    "preview": "wrangler pages dev public"
  },
  "devDependencies": {
    "wrangler": "^4.0.0"
  }
}
